import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'reto5_unidad3',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
